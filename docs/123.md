技术方案（一句话）
知识文档 → 切 chunk → 调用百炼 text-embedding-v4 得向量 → 写入 Chroma
用户问题 → 同一 API 向量化 → Chroma 相似度检索 → KnowledgeAnswerer（接口不变）→ LLM 归纳
组件	选型	是否本地安装
向量化
text-embedding-v4（DashScope OpenAI 兼容）
否，HTTP API
向量库
Chroma（data/chroma 持久化）
是，pip install chromadb
鉴权
复用 DASHSCOPE_API_KEY + DASHSCOPE_BASE_URL
与现有 LLM 一致
简历表述可改为：Chroma + 百炼 text-embedding-v4 云端向量化，避免写「本地 BGE」。

阶段 1 新步骤总览
步	内容	本步是否改业务
1
基线 + 理清 RAG 链路
否（你已完成）
2
只装 Chroma + 配置 embedding API
否
3
写 embedding.py（调百炼 API）
否
4
写 vector_store.py（Chroma 读写）
否
5
写 vector_retriever.py + reindex.py
否
6
改 retriever.py 按 RAG_BACKEND 切换
是
7
测试 + 调阈值
是
第 1 步（已完成）
pytest -q → 24 passed
能说出：Agent → KnowledgeAnswerer → KnowledgeBaseRetriever → SimpleKeywordIndex
知识库在 data/knowledge/
第 2 步（重做）：依赖 + API 配置（无本地模型）
2.1 要做什么
只增加 Chroma 依赖（不要 sentence-transformers）。
在 .env / settings 里声明 embedding 模型名、维度、后端开关。
确认 百炼 API Key 可用（与 LLM 同一套 Key 即可）。
用一条命令验证 embeddings API 能返回向量。
pytest 仍全部通过（此步还不改 retriever）。
2.2 修改 requirements.txt
在末尾只加一行：

chromadb>=0.5.0
不要加 sentence-transformers、torch。

2.3 安装
conda activate customer
cd D:\code4\llm-universe-main\customer_simple\customer_hand
pip install -r requirements.txt
python -c "import chromadb; print('chromadb OK', chromadb.__version__)"
安装体积会比「本地 BGE」小很多。

2.4 修改 .env.example（并在你的 .env 里同步）
在现有百炼配置下面追加：

# --- Vector RAG (Phase 1, API embedding) ---
# keyword = 现有关键词检索；chroma = 向量检索（第 6 步接入后再改为 chroma）
RAG_BACKEND=keyword
CHROMA_PERSIST_DIR=data/chroma
EMBEDDING_MODEL=text-embedding-v4
# v4 支持多维度，常用 1024（默认）；与建索引、查询必须一致
EMBEDDING_DIMENSIONS=1024
RAG_TOP_K=3
# 向量相似度阈值（cosine 距离下需实测，先 0.45 再调）
RAG_SCORE_THRESHOLD=0.45
# 重建索引 / 查询向量化是否要调 API（测试可 false + mock）
EMBEDDING_ENABLED=true
说明：

DASHSCOPE_API_KEY：继续用你已有的，不必新 Key。
EMBEDDING_ENABLED：本地开发可 true；CI 里可 false 并对 embedding 做 mock（第 7 步）。
RAG_BACKEND=keyword：第 6 步前保持旧行为，避免半成品向量库导致误报。
2.5 修改 app/settings.py
在 knowledge_dir 后增加字段，例如：

rag_backend: str = Field(default="keyword")
chroma_persist_dir: Path = Field(default=PROJECT_ROOT / "data" / "chroma")
embedding_model: str = Field(default="text-embedding-v4")
embedding_dimensions: int = Field(default=1024)
embedding_enabled: bool = Field(default=True)
rag_top_k: int = Field(default=3)
rag_score_threshold: float = Field(default=0.45)
field_validator 里把 chroma_persist_dir 和 flow_dir、knowledge_dir 一起做路径解析。

2.6 验证百炼 Embedding API（本步最重要）
与项目里 LLMClient 一样，用 OpenAI 兼容 的 embeddings.create：

python -c "
from dotenv import load_dotenv
from openai import OpenAI
import os
load_dotenv('.env')
client = OpenAI(
    api_key=os.getenv('DASHSCOPE_API_KEY') or os.getenv('BAILIAN_API_KEY'),
    base_url=os.getenv('DASHSCOPE_BASE_URL', 'https://dashscope.aliyuncs.com/compatible-mode/v1'),
)
r = client.embeddings.create(
    model='text-embedding-v4',
    input='退货规则',
    dimensions=1024,
    encoding_format='float',
)
vec = r.data[0].embedding
print('embedding OK', len(vec), vec[0])
"
期望：embedding OK 1024 ...（维度与 EMBEDDING_DIMENSIONS 一致）。

若失败，常见原因：Key 未填、地域 URL 不对、账户未开通 embedding 模型。

2.7 本步验收

 chromadb 可 import

 上面 embedding 脚本成功

 settings.embedding_model 为 text-embedding-v4

 pytest -q 仍 24 passed（未改 retriever 时应不变）
第 3 步：实现 app/rag/embedding.py（API 封装）
职责
读 settings：embedding_model、embedding_dimensions、embedding_enabled、API Key / Base URL（与 LLMClient.from_env() 同源逻辑）。
embed_query(text) -> list[float]
embed_documents(texts) -> list[list[float]]
批量：text-embedding-v4 单次最多 10 条，超过要分批 embeddings.create。
设计要点（新手易错）
# 伪代码结构
class EmbeddingClient:
    def embed_documents(self, texts: list[str]) -> list[list[float]]:
        for batch in chunks(texts, size=10):
            call API ...
    def embed_query(self, query: str) -> list[float]:
        return self.embed_documents([query])[0]
embedding_enabled=false 时：抛明确异常或返回空，供测试 mock。
不要把 API Key 写进代码；错误信息里脱敏 Key。
本步验收
单独运行小脚本：embed_query("退货规则") 长度 1024。
仍不改 KnowledgeAnswerer。
第 4 步：app/rag/vector_store.py（Chroma）
职责
用 PersistentClient(path=settings.chroma_persist_dir)。
Collection 名建议固定，如 knowledge_chunks。
upsert(chunks)：id、document、embedding、metadata（source 等）。
query(embedding, top_k) → [{text, source, score, metadata}, ...]。
距离与分数
Chroma 常用 cosine；返回的 distance 要统一换算成对外 score（越大越相似或越小越相似，全项目用一种，并在注释里写清）。

本步验收
手动 upsert 2～3 条假向量能 query 到（可用假向量先测库逻辑）。
第 5 步：vector_retriever.py + reindex.py
reindex.py
读 data/knowledge（复用现有 KnowledgeDocumentLoader + TextSplitter）。
embedding_client.embed_documents(所有 chunk 文本) → 会消耗 Token，重建索引前心里要有数。
写入 Chroma。
暴露 rebuild_index() -> dict（chunk 数、耗时等）。
vector_retriever.py
retrieve(query, top_k)：先 embed_query，再 Chroma 查。
按 RAG_SCORE_THRESHOLD 过滤低分。
返回结构尽量对齐现有 RetrievalMatch / KnowledgeChunk，方便 answerer 少改。
可选 API（计划里已有）
POST /api/knowledge/reindex
GET /api/knowledge/status
可放在第 6 步一起做。

本步验收
python -c "from app.rag.reindex import rebuild_index; print(rebuild_index())"
再查「退货规则」能命中 shop_faq.md 相关片段（需 API Key 有效）。

第 6 步：改 retriever.py 切换后端
# 逻辑示意
if settings.rag_backend == "chroma":
    # VectorKnowledgeRetriever（内部用 vector_retriever）
else:
    # 现有 SimpleKeywordIndex
然后把 .env 里：

RAG_BACKEND=chroma
执行一次 rebuild_index()，再：

pytest -q
uvicorn main:app --reload
在 Swagger 里问「退货规则」，看 matches 里是否有 source、score。

接口不变：

KnowledgeAnswerer().answer("退货规则", top_k=3)
第 7 步：测试与调参
新增 test/test_vector_rag.py
场景	做法
不想每次 CI 调 API
monkeypatch / mock EmbeddingClient.embed_* 返回固定向量
集成测（可选）
EMBEDDING_ENABLED=true 且本地有 Key 时跑
覆盖：reindex、检索命中、无命中兜底、matches 字段、answer() 签名不变。

调参
RAG_SCORE_THRESHOLD：API 向量分数刻度与关键词不同，要用真实问句试。
chunk 大小：仍用现有 TextSplitter，效果不好再调。
EMBEDDING_DIMENSIONS：改维度必须 清空 Chroma 重建索引。
与「本地 BGE」方案对比（面试可讲）
维度	本地 BGE	text-embedding-v4 API
依赖
torch + 数 GB
仅 chromadb + openai（已有）
离线
可离线检索
建索引、查询都要联网（除非缓存向量）
成本
算力在本机
按 Token 计费
Docker
镜像大、下模型慢
镜像小，需配 Key
中文效果
BGE 专精中文
v4 多语种，电商 FAQ 通常够用
测试
易本地跑
需 mock embedding
你现在该做哪一步
你已完成 新第 1 步。请按上面 新第 2 步 操作：

requirements.txt 只加 chromadb
更新 .env.example / .env / settings.py
跑 embedding API 验证脚本
pytest -q 确认仍 24 passed
完成后把这三条结果发我，我再按你的环境写 第 3 步 embedding.py 的完整代码结构（可对齐现有 LLMClient 风格）：

embedding 验证脚本输出
pytest -q 结果
是否需要我直接在仓库里改 requirements.txt、settings.py、.env.example（你本地执行 pip install 即可）